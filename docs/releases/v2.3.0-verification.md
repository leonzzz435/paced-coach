# v2.3.0 verification — candidate in progress

Started 2026-09-16 from public main
`2664b5e333670cc33f7a3e9cdd10b27560fbe529`. This is not release certification.
Runtime code at `b189943fc2212fe49e960520a8711dc21c8b4003` passed
[GitHub CI](https://github.com/leonzzz435/paced-coach/actions/runs/35137946954)
and the complete local release audit. Subsequent documentation commits must pass
their own exact-commit checks before release; current candidate status is tracked
in [PR #13](https://github.com/leonzzz435/paced-coach/pull/13).

## Completed locally

| Check | Observed result |
| --- | --- |
| Python dependency refresh | `pip-audit` reported no known advisories in the installed Linux environment |
| npm dependency refresh | `npm audit` reported zero vulnerabilities |
| Python style and types | Ruff passed; mypy checked 207 source files |
| Full test task after runtime and audit fixes | 431 Python tests passed, one optional destructive integration test skipped; frontend contract tests and type check passed |
| Optional active-plan DB integration | Run separately against disposable PostgreSQL: one passed |
| Frontend | Lint, type check, contract tests and production build passed on Next.js 16.3.5 |
| Browser suite | Fourteen desktop/mobile scenarios passed against synthetic fixtures and mocked API data, including delayed profile loading/saving and failed-load recovery |
| Agents API | One synthetic no-environment Astra turn completed; persisted response checked; session deleted |
| Demo capture | Four current screenshots and a local WebM recorded; capture blocked app reads, writes and external requests |
| Docker build context | A real image export excluded eight synthetic private-file sentinels while retaining public examples and source |
| Source archives and content audit | Regression tests verify public environment examples survive archiving and tracked content is scanned even when excluded from archives |
| Local network boundary | Real development and production frontend starts listened on `127.0.0.1`; Docker host ports remained loopback-bound |
| Release audit | Clean-tree, tracked-content, all reachable-ref history, remote freshness, workflow, generated-path and network checks passed |
| GitHub reporting | Private vulnerability reporting enabled; secret scanning and push protection already enabled |
| Local privacy reset | Browser disabled-state, server guard, missing-origin guard, cancellation and confirmed reset checked against a copy of the synthetic acceptance database; application/checkpoint rows cleared and technical owner preserved |

These are point-in-time observations. Advisory databases change. Linux execution
does not certify macOS or Windows installation, and scanners do not prove that
code is free of vulnerabilities. A Starlette/AnyIO deprecation warning remains
in the test environment; it did not fail tests.

A second history scan without repository allowlists produced one finding: an
inspected synthetic UI block identifier in an old fixture, not a credential.
This explains the applicable scanner exception; it does not extend the scan to
unreachable Git objects, private databases or external service logs.

## Live Astra lifecycle

The live test used a separate local PostgreSQL database, Redis instance, API and
worker. It used an existing OpenAI credential but no real athlete data. Tracing
was disabled. The synthetic athlete declared four runs a week, weekday limits,
three unavailable days and a half-marathon goal.

Observed so far:

1. Profile and competition were entered and saved through the real browser UI.
2. The coach asked whether race Sunday was an exception to the ordinary
   40-minute Sunday limit.
3. API and worker were stopped and recreated while the job awaited an answer.
4. The same clarification remained available. An answer submitted through the
   browser resumed the same job.
5. The job completed and published schema-v3 season and execution plans.
6. The plan contained 28 consecutive days. All daily durations respected the
   declared limits; Tuesday, Thursday and Friday had no sessions.
7. The generated plan rendered in the real UI without captured JavaScript errors.
8. A proposed Wednesday reduction left version 1 and the 40-minute day unchanged
   until acceptance. Accepting saved version 2 with 25 minutes on that day only;
   the other 27 days and season roadmap were unchanged.
9. A second proposed change was rejected through the browser. The complete active
   plan response remained identical before and after rejection.
10. After fixing the response-envelope tool limit, live memory-model requests
    completed without the earlier limiter exception.

The live pass exposed and fixed two runtime defects: structured answer envelopes
incorrectly consumed zero-tool budgets, and untyped JSON metadata prevented
proposal acceptance on asyncpg. Regression tests now exercise the real agent
framework and PostgreSQL boundary. Public demo checks also caught legacy device
metrics, eager local-app navigation and persistence actions in the preview;
the updated browser tests and guarded capture verify their removal.

This is one observed scenario, not a coaching-quality benchmark. Cost and token
measurements were not established for the LangGraph run; no cost reduction or
model-superiority claim is made. The separate Agents API smoke test does not
replace application acceptance testing.

## Fresh local setup and profile regression

An additional checkout on Ubuntu 24.04 used the public `make setup` and
`make start` path, Node 24, Docker Compose, fresh application dependencies and
uniquely named disposable volumes. Only the existing OpenAI key was supplied;
optional tracing remained disabled. No original athlete database was used.

The first pass exposed a profile-form race: inputs were editable before the
initial profile request finished. Its delayed response replaced the entered
constraints with defaults before saving. The resulting plan was not accepted as
valid test evidence. Reading back the stored profile identified the lost input;
this was not evidence that the model ignored a correctly supplied profile.

After the fix, the form waits for a successful load, prevents edits during saves
and offers a safe retry after failed loading. The repeat used new disposable
volumes and verified the saved profile over the API before generation. Default
GPT-5.5 completed schema-v3 season and execution artifacts with 28 days, only
running sessions, all daily limits respected and all twelve unavailable days
free of sessions. The block rendered without captured JavaScript errors.

The coach then correctly identified Saturday's 70-minute run and its fit within
the declared 80-minute window, without measured-device claims or plan changes.
After restarting PostgreSQL, Redis, API, worker and beat, the complete active
plan response remained identical (season and execution version 1), and both
messages in the coach exchange were still present.

The host prerequisites were already installed; this was a clean application
checkout and services, not an independently provisioned operating-system image.
Pixi's deprecated `project` field and Celery's root-container-user warning were
observed during startup; neither prevented installation or execution.

## Bounded model comparison

The same synthetic 181-token constraint prompt was sent once to each model via
the application's model selector, with medium reasoning and research disabled.
Both models asked about a race-day exception, stayed within the stated weekly
availability, and avoided invented wearable measurements or claims of saving a
plan. GPT-5.5 returned in 10.82 seconds with 686 output tokens (404 reasoning);
Astra returned in 17.16 seconds with 478 output tokens (254 reasoning).

This was one standalone prompt per model, outside the full coaching workflow.
It does not establish relative quality, typical latency, or cost. The full live
application lifecycle above used Astra; an equivalent GPT-5.5 lifecycle was not
run as part of this comparison.

## Final operational-copy and reset pass

The core candidate was merged through PR #13 as
`f0a407593bb4b866fd6312e925df1934b711feb4`, with
[exact-main CI](https://github.com/leonzzz435/paced-coach/actions/runs/35140076854)
and a repeated clean-commit release audit passing. At that check, GitHub reported
zero open Dependabot and secret-scanning alerts; the three superseded dependency
PRs closed automatically. The unpublished v2.3.0 draft was created against that
commit. Later candidate changes require their own checks and draft-target update.

A follow-up corrected retired hosted-sign-in and training-connector descriptions
on the local data/support pages and mobile account menu. All five legal pages
returned HTTP 200 and displayed operational-draft status. This engineering copy
review preserves existing business facts and does not replace external review.

Reset acceptance used a separate, read-only volume copy of the disposable
synthetic GPT-5.5 acceptance database, with no live model key or worker running.
The disabled UI button, the API's disabled-reset response and the frontend's
missing-origin write guard were checked. After explicitly enabling the flags,
cancelling the browser confirmation preserved the complete plan. Confirming
issued one delete request and navigated to the correct local-reset success page.
Database counts showed that all populated application tables were cleared,
including 31 checkpoint/blob/write rows, while the same technical owner and
migration records remained. No JavaScript page errors were captured.

This checks completed-run data in the active database. It does not establish
behavior during an in-flight model job or erase backups, logs, filesystem
exports, or data held by external providers. One verifier attempt lost access
to the response body during the immediate redirect; the final successful repeat
used a fresh copy and verified the page and database directly.

The published v2.2.0 release description was corrected to state its actual
August 2 publication date and label its evidence as historical. Its tag, target
commit, publication date and published state were preserved.

## Still required

- Record the final candidate identity and repeat audits/CI after final edits.
- Obtain the outstanding external legal review before broad public distribution.

The deliverable is self-hosted open-source code. A separate website, managed
deployment, hosted authentication and hosted billing are outside this release.

Raw evidence and recordings remain in ignored local `.tmp/` directories. Public
assets must be deliberately selected and reviewed. No article or social post has
been published as part of this candidate.
