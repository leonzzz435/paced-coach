# Schema v3 demo fixtures

Synthetic, identity-free fixtures for the canonical Head Coach artifacts. Schema v3 is intentionally available for season and weekly execution plans only; analysis remains on its v1 contract.

The fixtures contain Markdown and typed semantic blocks, never model-authored HTML or CSS. They are reviewed examples for renderer and release validation, not exports of a real athlete run.
